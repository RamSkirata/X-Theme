/**
 * Main theme script.
 *
 * Keep JavaScript minimal by default to avoid conflicts with Elementor widgets.
 */
(function () {
	'use strict';

	// Add a class once JS is available for progressive enhancement if needed.
	document.documentElement.classList.add('js');
})();